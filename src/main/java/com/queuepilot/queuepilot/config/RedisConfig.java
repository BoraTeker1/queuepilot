package com.queuepilot.queuepilot.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration for Redis integration.
 * Used for idempotency keys, rate limiting, and hot incident state.
 */
@Configuration
public class RedisConfig {
    // TODO: Configure Redis connection and templates
}
