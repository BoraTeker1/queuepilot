# PostgreSQL Configuration
POSTGRES_DB=queuepilot
POSTGRES_USER=queuepilot
POSTGRES_PASSWORD=queuepilot

# Redis Configuration (optional - defaults work for local dev)
# REDIS_HOST=localhost
# REDIS_PORT=6379

# Spring Boot Application Configuration
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/queuepilot
# SPRING_DATASOURCE_USERNAME=queuepilot
# SPRING_DATASOURCE_PASSWORD=queuepilot
